
<div class="container">
  <h3>Qu'en penses-tu ?</h3>
  <div class="form-create">
    <%= simple_form_for([ @beer, @review ]) do |f| -%>
      <%= f.input :title, label: 'Titre de ton commentaire', placeholder: "Ton titre ici" %>
      <%= f.input :content, label: 'Que veux-tu raconter?', placeholder: "Ton commentaire ici" %>
      <%= f.input :rate, label: 'Une note peut-être?', collection: Review::STARS, prompt: "Ta note" %>
      <%= f.submit class: "submit-btn", value: "ajouter" %>
    <% end %>
  </div>
  <%= render 'shared/footer' %>
</div>

